# Dashboard Sync Configuration
# Copy this file to .dashboard-sync.env and fill in the values

# GAS Web App URL (Required)
# Get this from: clasp deploy --deploymentId <ID>
GAS_DASHBOARD_URL="https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec"

# API Key for authentication (Optional but recommended)
# Set this in GAS Script Properties as SYNC_API_KEY
GAS_SYNC_API_KEY="your-secure-api-key"
